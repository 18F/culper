<ContactInformation Version="1" Type="Pooled">
  {{- range $index, $item := .props.Emails.props.items}}
  {{- with $Item := $item.Item}}
  <!-- XXX See https://github.com/18F/e-QIP-prototype/issues/781 -->
  {{if $index  | eq 0}}
  <HomeEmail>
    <Email>{{text $Item.Email}}</Email>
  </HomeEmail>
  {{end}}
  {{if $index  | eq 1}}
  <WorkEmail>
    <Email>{{text $Item.Email}}</Email>
  </WorkEmail>
  {{end}}
  {{- end}}
  {{- end}}
  {{- range $index, $item := .props.PhoneNumbers.props.items}}
  {{- with $Item := $item.Item}}
  <!-- XXX See https://github.com/18F/e-QIP-prototype/issues/761 -->
  {{if $index | eq 0}}
  <HomeTelephone>
    <Telephone>{{- telephone $Item.Telephone -}}</Telephone>
  </HomeTelephone>
  {{end}}
  {{if $index | eq 1}}
  <MobileTelephone>
    <Telephone>{{- telephone $Item.Telephone -}}</Telephone>
  </MobileTelephone>
  {{end}}
  {{if $index | eq 2}}
  <WorkTelephone>
    <Telephone>{{- telephone $Item.Telephone -}}</Telephone>
  </WorkTelephone>
  {{end}}
  {{- end}}
  {{- end}}
</ContactInformation>
